apiVersion: v1
kind: Service
metadata:
  name: {{ include "empreinte.fullname" . }}
  labels:
    {{- include "empreinte.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "empreinte.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
